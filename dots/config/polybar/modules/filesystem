[module/filesystem]
type = internal/fs
interval = 25
mount-0 = /

; Menampilkan mountpoint dan ukuran terpakai / total dengan desimal
label-mounted = %used:disk% / %total:disk%

label-unmounted = %mountpoint% not mounted
label-unmounted-foreground = ${colors.disabled}
